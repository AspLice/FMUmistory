--[[
    config.lua - CR海物語 パチンコ設定ファイル
    大当たり確率、配当、確変設定などを集中管理する。
    サーバー・クライアント共有スクリプトとして読み込まれる。
]]

Config = {}

-- =========================================================
-- ゲームの基本設定
-- =========================================================

-- パチンコ台を使用するためのアイテム名（QBCore インベントリ）
-- nil の場合は現金（cash）を使用する
Config.BallItem = nil  -- 例: 'pachinko_ball'

-- 1玉あたりのコスト（現金モード時）
Config.BallCostCash = 4  -- 4ドル/玉 (= 4円換算)

-- 玉の最大保有数（1回の開始時に購入できる最大数）
Config.MaxBallsPerSession = 500

-- チャッカー入賞時に消費する玉数
Config.BallsPerChakker = 1

-- =========================================================
-- 抽選確率設定
-- =========================================================

Config.Lottery = {
    -- 【通常時】大当たり確率 (分母)
    -- 例: 319 → 1/319 の確率で当たる
    normalDenominator = 319,

    -- 【確変時（高確率）】大当たり確率 (分母)
    -- 例: 39 → 1/39 の確率で当たる（約8倍）
    kakuhenDenominator = 39,

    -- リーチ成立確率（チャッカー入賞時、ハズレでもリーチが発生する割合）
    -- 通常時: 1/8 程度
    reachProbabilityNormal = 8,
    -- 確変時はリーチが頻繁に起きる
    reachProbabilityKakuhen = 3,

    -- スーパーリーチ発生確率（リーチ成立後、さらにスーパーに発展する確率 分母）
    superReachDenominator = 4,

    -- 魚群演出発生確率（スーパーリーチ時のみ判定）
    -- 魚群が出現した場合の大当たり期待度: 約80%
    gyogunProbabilityOnSuperReach = 3,  -- 1/3 の確率で魚群

    -- 大当たり時の図柄選択
    -- 奇数図柄(1,3,5,7,9) → 確変大当たり
    -- 偶数図柄(2,4,6,8)   → 通常大当たり
    -- ※内部的にサーバーが図柄を決定する
}

-- =========================================================
-- 大当たり・ラウンド設定
-- =========================================================

Config.Jackpot = {
    -- 1ラウンドあたりの開放時間（秒）
    roundDurationSec = 25,

    -- 通常大当たりのラウンド数（通常: 4R または 15R）
    normalRounds = {4, 15},   -- この配列からランダム選択
    normalRoundsWeight = {30, 70},  -- 重み (4R: 30%, 15R: 70%)

    -- 確変大当たりのラウンド数
    kakuhenRounds = {15},

    -- 1ラウンドあたりの獲得玉数（払い出し）
    ballsPerRound = 14,
}

-- =========================================================
-- 確変（高確率）設定
-- =========================================================

Config.Kakuhen = {
    -- 確変継続率（次の大当たりまで確変が続く確率）
    continuationRate = 1.0,  -- 1.0 = 必ず継続（いわゆる「V確変」）

    -- 確変の最大継続回転数（転落なし方式の場合は不要）
    -- maxSpins = nil の場合は次の大当たりまで継続
    maxSpins = nil,

    -- 確変中の時短（電チュー保留変動）
    -- 時短中は自動的に確率が上がる演出表示
    jitan = true,
}

-- =========================================================
-- 配当設定（大当たり時の払い出し）
-- =========================================================

Config.Payout = {
    -- 獲得玉を現金に変換するレート（1玉 = X ドル）
    -- 例: 0.25 → 100玉で25ドル
    ballToCashRate = 0.25,

    -- 確変大当たり時のボーナス倍率
    kakuhenBonusMultiplier = 1.5,

    -- 払い出し通貨タイプ ('cash', 'bank', 'crypto')
    moneyType = 'cash',
}

-- =========================================================
-- セキュリティ設定
-- =========================================================

Config.Security = {
    -- チャッカー入賞の最小間隔（秒）
    -- この時間内に連続入賞通知が来た場合はチートとみなす
    minChakkerIntervalSec = 0.3,

    -- 1分あたりの最大入賞回数（通常プレイで物理的に不可能な数値）
    maxChakkerPerMinute = 100,

    -- 最大累積チート検知数（この回数を超えるとキック対象）
    maxViolations = 5,

    -- バン時間（分）- 0 の場合はキックのみ
    banMinutes = 60,
}

-- =========================================================
-- NUI・インターフェース設定
-- =========================================================

Config.UI = {
    -- チャッカー入賞後、スロットが回り始めるまでの遅延（ミリ秒）
    slotStartDelayMs = 500,

    -- スロットの最初の2列が止まった後、3列目が止まるまでの最大時間（ms）
    -- リーチ演出中はこの時間を引き伸ばす
    reachWaitMs = {
        normal  = 2000,
        reach   = 5000,
        super   = 10000,
    },

    -- FPS制限（Matter.js物理エンジンの更新レート）
    physicsTargetFPS = 60,

    -- 魚群演出の継続時間（ms）
    gyogunDurationMs = 4000,
}

-- =========================================================
-- 台の配置座標（ワールド内でのコマンド発動地点）
-- =========================================================
-- 複数台設置する場合はここにベクターを追記する
Config.MachineLocations = {
    {
        coords   = vector3(0.0, 0.0, 0.0),  -- TODO: 実際の座標に変更すること
        heading  = 0.0,
        label    = '1番台',
    },
}

-- インタラクション距離（メートル）
Config.InteractDistance = 2.0
